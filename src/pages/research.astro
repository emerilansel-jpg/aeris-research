---
// research.astro — Research index page
import BaseLayout from '../layouts/BaseLayout.astro';
import ArticleCard from '../components/ArticleCard.astro';
import { getCollection } from 'astro:content';

const allPosts = await getCollection('research');
const posts = allPosts.sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);
---

<BaseLayout
  title="Research & Analysis | Aeris Research"
  description="Browse all independent technology research articles and analysis from Aeris Research — covering AI, cloud, cybersecurity, SaaS, and fintech."
>
  <section class="section" aria-labelledby="research-page-title">
    <div class="container">
      <div class="section__header">
        <p class="section__eyebrow">Research Library</p>
        <h1 class="section__title" id="research-page-title">Technology Research &amp; Analysis</h1>
      </div>

      {posts.length > 0 ? (
        <div class="article-grid">
          {posts.map(post => (
            <ArticleCard
              title={post.data.title}
              slug={post.id}
              category={post.data.category}
              description={post.data.description}
              author={post.data.author}
              date={post.data.date}
              featured_image={post.data.featured_image}
            />
          ))}
        </div>
      ) : (
        <p style="color: var(--text-muted); font-family: 'Inter', sans-serif;">
          No research articles published yet.
        </p>
      )}
    </div>
  </section>
</BaseLayout>
